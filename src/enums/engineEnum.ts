enum EngineEnum {
  STARTED = 'started',
  STOPPED = 'stopped',
  DRIVE = 'drive',
}

export default EngineEnum;

export enum EngineStatusEnum {
  STARTED = 'started',
  RUNNING = 'running',
  STOPPED = 'stopped',
  BROKEN = 'broken',
}
